name: "Test to run workflow manually"
run-name: "Execution build v16"

on:
  workflow_dispatch:


jobs:
  secure-job:
    runs-on: ubuntu-latest
    steps:
      - name: "Attempting to print the secret"
        run: |
          echo "The secret toke from the vault is : ${{ secrets.MY_AWS_FAKE_TOKEN   }}
